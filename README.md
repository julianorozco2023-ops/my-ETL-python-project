#ETL-python-project
Extract, Transformation and loading of data from AlphaAdvantage. Used Python to extract and transform data to then load data into an SQL database. Used SQL to correctly acquire the data needed or wanted. 
